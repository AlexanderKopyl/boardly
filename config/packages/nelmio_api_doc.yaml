nelmio_api_doc:
  documentation:
    openapi: '3.1.0'
    info:
      title: Boardly API
      description: Boardly project and task management API
      version: 0.1.0
    components:
      securitySchemes:
        bearerAuth:
          type: http
          scheme: bearer
          bearerFormat: JWT
  areas:
    path_patterns:
      - ^/api/(?!doc$)
