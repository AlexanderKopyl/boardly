description = "Plan a Boardly task using the existing task-planning skill"
prompt = """
Plan a Boardly task.

Use GEMINI.md as the default rule map.
Before creating a plan, load and follow `.codex/skills/task-planning/SKILL.md`.
Do not duplicate or reinterpret that workflow here.
Load only additional task-relevant docs or skills.

If the task folder, goal, scope, or target area requirements are unclear, follow the rules from `.codex/skills/task-planning/SKILL.md`.
Stop if that skill cannot be loaded.

Return the summary required by the skill and identify the saved planning artifact.
"""
