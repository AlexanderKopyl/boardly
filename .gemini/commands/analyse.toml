description = "Analyze a Boardly task using Gemini skills"
prompt = """
Analyze a Boardly task.

Use GEMINI.md as the default rule map.
Use `.gemini/skills/task-analysis/SKILL.md` for backend, infrastructure, docs, or mixed tasks.
Use `.gemini/skills/frontend-task-analysis/SKILL.md` for frontend-only tasks.
Do not use `.codex/skills/*` as runtime instructions for Gemini.
Load only additional task-relevant docs or skills.

If the task folder or target area is unclear, follow the selected Gemini skill rules.
Stop if the selected Gemini skill cannot be loaded.

Return the summary required by the selected skill and identify the saved analysis artifact when one is created.
"""
