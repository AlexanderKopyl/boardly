description = "Implement a Boardly task using Gemini skills"
prompt = """
Implement a Boardly task.

Use GEMINI.md as the default rule map.
Use `.gemini/skills/task-implementation/SKILL.md` for backend, infrastructure, docs, or mixed tasks.
Use `.gemini/skills/frontend-task-implementation/SKILL.md` for frontend-only tasks.
Do not use `.codex/skills/*` as runtime instructions for Gemini.
Load only additional task-relevant docs or skills.

If the task folder, plan, checklist, or target area is unclear, follow the selected Gemini skill rules.
Stop if the selected Gemini skill cannot be loaded.

Return the summary required by the selected skill.
"""
